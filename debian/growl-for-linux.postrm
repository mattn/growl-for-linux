#!/bin/sh
set -e

rmdir_() {
    ! [ -d "$1" ] || rmdir --ignore-fail-on-non-empty "$1"
}

case "$1" in

  remove)
    rmdir_ /usr/lib/growl-for-linux/display
    rmdir_ /usr/lib/growl-for-linux/subscribe
    ;;

esac

